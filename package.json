{
  "name": "rejected.us",
  "version": "1.0.1",
  "description": "Everyone’s been rejected - these are our stories",
  "private": true,
  "homepage": "https://rejectedus.github.io/rejected.us",
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "prettier": "^1.6.1",
    "react-scripts": "1.0.13"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "styled-components": "^2.0.0",
    "styled-normalize": "^2.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "format":
      "prettier --single-quote --trailing-comma es5 --write '{src,public}/**/*.js'",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
